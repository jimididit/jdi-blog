---
import CodeCopy from './CodeCopy.svelte'

interface Props {
  language?: string;
}

const { language = 'text' } = Astro.props;
---
<CodeCopy client:load/>
<code class="astro-ink__code" data-language={language}>
    <slot/>
</code>